(update_translations.py is a source file, and the language JSON files being added/modified/deleted are also source/config files, so no build artifacts, dependencies, or temp files are present in the changes)